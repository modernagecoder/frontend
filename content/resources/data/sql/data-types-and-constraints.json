{
  "title": "Data Types and Constraints (NOT NULL, UNIQUE, PRIMARY KEY)",
  "slug": "data-types-and-constraints",
  "language": "sql",
  "chapter": 3,
  "difficulty": "Beginner",
  "readingTime": "24 min",
  "lastUpdated": "2026-04-11",
  "totalQuestions": 56,
  "seo": {
    "title": "SQL Data Types and Constraints - INT, VARCHAR, DATE, DECIMAL, PRIMARY KEY, FOREIGN KEY | Modern Age Coders",
    "description": "Complete guide to MySQL data types — numeric (INT, BIGINT, DECIMAL), string (CHAR, VARCHAR, TEXT), date/time (DATE, DATETIME, TIMESTAMP), and ENUM. Learn constraints: NOT NULL, UNIQUE, PRIMARY KEY, FOREIGN KEY, CHECK, DEFAULT, AUTO_INCREMENT. 56 practice questions with real-world scenarios.",
    "keywords": "sql data types, mysql data types, int vs bigint, varchar vs char, decimal for money, date vs datetime vs timestamp, primary key mysql, foreign key mysql, auto_increment, not null unique check default, composite primary key, phone number varchar"
  },
  "content": {
    "what": "<h2>What Are Data Types and Constraints?</h2><p>When you create a table, every column has two things attached to it: a <strong>data type</strong> and optionally one or more <strong>constraints</strong>.</p><ul><li>A <strong>data type</strong> says what kind of values the column can hold — integers, strings, dates, etc. It controls storage size and valid operations.</li><li>A <strong>constraint</strong> is a rule that limits which values are allowed — no NULLs, no duplicates, values between 0 and 100, etc. Constraints protect your data from garbage getting in.</li></ul><p>Picking the right data type and constraints is half of good database design. A column declared as <code>VARCHAR(500)</code> when 20 characters is the true max wastes memory and lets bad data slip in. A phone number stored as <code>INT</code> loses leading zeros and breaks for international numbers. A salary column without <code>NOT NULL</code> lets someone insert a rogue row with an empty salary that crashes your reports.</p><h3>Sample Tables for This Chapter</h3><p>We will refer to two tables throughout this chapter.</p><p><strong>students</strong></p><table><tr><th>student_id</th><th>name</th><th>email</th><th>dob</th><th>cgpa</th></tr><tr><td>1</td><td>Aarav Sharma</td><td>aarav@college.edu</td><td>2005-03-15</td><td>8.50</td></tr><tr><td>2</td><td>Priya Iyer</td><td>priya@college.edu</td><td>2004-11-22</td><td>9.20</td></tr><tr><td>3</td><td>Rohan Verma</td><td>rohan@college.edu</td><td>2005-07-08</td><td>7.80</td></tr></table><p><strong>orders</strong></p><table><tr><th>order_id</th><th>student_id</th><th>amount</th><th>order_date</th></tr><tr><td>5001</td><td>1</td><td>1200.50</td><td>2026-04-01 14:30:00</td></tr><tr><td>5002</td><td>2</td><td>850.00</td><td>2026-04-02 09:15:00</td></tr></table>",
    "why": "<h2>Why Types and Constraints Matter</h2><h3>1. Data Integrity — Your Database Fights Bugs For You</h3><p>Without constraints, any bad row can sneak in: a student with no name, a duplicate email, a negative salary, a date of birth in the year 3000. Once the bad data is in, downstream reports break and customer support has to clean it up. With constraints, the database rejects the bad row at INSERT time. You fix the bug once (in the schema) instead of a hundred times (in every query).</p><h3>2. Storage Efficiency</h3><p>A <code>TINYINT</code> uses 1 byte. A <code>BIGINT</code> uses 8 bytes. On a 50-million-row table, using BIGINT instead of TINYINT for a \"gender\" or \"status\" column wastes 350MB for no benefit. Multiply that across dozens of columns and multiple tables, and you pay for it in RAM, disk, and backup size.</p><h3>3. Performance Hinges on Primary Keys and Indexes</h3><p>Every row lookup, JOIN, and filter uses indexes. Indexes are built on primary keys and constraints. Choosing the right primary key determines whether your query runs in 10ms or 10 seconds on a million rows.</p><h3>4. Interview-Favorite Topic</h3><p>\"What is the difference between CHAR and VARCHAR?\", \"When would you use DECIMAL instead of FLOAT?\", \"What is the difference between a PRIMARY KEY and a UNIQUE constraint?\", \"Can a table have multiple UNIQUE constraints but only one PRIMARY KEY?\" — these are asked in almost every SQL interview at Flipkart, Amazon, Zomato, and countless startups.</p>",
    "explanation": "<h2>Detailed Explanation</h2><h3>1. Numeric Types</h3><table><tr><th>Type</th><th>Bytes</th><th>Range (signed)</th><th>Typical Use</th></tr><tr><td><code>TINYINT</code></td><td>1</td><td>-128 to 127</td><td>Flags, small status codes</td></tr><tr><td><code>SMALLINT</code></td><td>2</td><td>-32,768 to 32,767</td><td>Counts in small range</td></tr><tr><td><code>MEDIUMINT</code></td><td>3</td><td>-8M to 8M</td><td>Rarely used</td></tr><tr><td><code>INT</code></td><td>4</td><td>-2.1B to 2.1B</td><td>IDs, counts (most common)</td></tr><tr><td><code>BIGINT</code></td><td>8</td><td>-9.2 quintillion to 9.2 quintillion</td><td>Huge counts, file sizes</td></tr><tr><td><code>DECIMAL(p, s)</code></td><td>variable</td><td>exact</td><td>Money, precision math</td></tr><tr><td><code>FLOAT</code></td><td>4</td><td>~7-digit precision</td><td>Scientific, approx</td></tr><tr><td><code>DOUBLE</code></td><td>8</td><td>~15-digit precision</td><td>Scientific, approx</td></tr></table><h4>DECIMAL vs FLOAT — Critical for Money</h4><p><code>DECIMAL(10, 2)</code> means \"up to 10 total digits, 2 after the decimal point\" — so values from -99,999,999.99 to 99,999,999.99 stored EXACTLY. Use for money, accounting, tax calculations.</p><p><code>FLOAT</code> and <code>DOUBLE</code> store values in binary floating-point, which cannot exactly represent 0.1 or 0.2. So <code>0.1 + 0.2 = 0.30000000000000004</code> in floats. Fine for physics simulations, fatal for a bank ledger where pennies must add up exactly.</p><p><strong>Rule: if it's money or currency, always use DECIMAL.</strong></p><h3>2. String Types</h3><table><tr><th>Type</th><th>Storage</th><th>Max</th><th>When to Use</th></tr><tr><td><code>CHAR(n)</code></td><td>fixed n chars, padded</td><td>255</td><td>Fixed-length codes (country='IN', pincode, gender='M')</td></tr><tr><td><code>VARCHAR(n)</code></td><td>actual length + 1-2 bytes</td><td>65,535</td><td>Names, emails, anything variable</td></tr><tr><td><code>TEXT</code></td><td>up to 64KB</td><td>65,535</td><td>Blog posts, long notes</td></tr><tr><td><code>MEDIUMTEXT</code></td><td>up to 16MB</td><td>~16M</td><td>Articles, product descriptions</td></tr><tr><td><code>LONGTEXT</code></td><td>up to 4GB</td><td>~4B</td><td>Books, huge logs</td></tr></table><h4>CHAR vs VARCHAR — When to Use Which</h4><p>Use <code>CHAR(n)</code> for columns where the value is always exactly n characters long: country codes (2 chars), UPI codes, MD5 hashes (32 chars), pin codes (6 chars in India). CHAR is slightly faster because the row has a fixed size.</p><p>Use <code>VARCHAR(n)</code> for everything else: names, addresses, emails, product descriptions. VARCHAR only uses the space it needs.</p><p>Use <code>TEXT</code> when you need more than 65,535 characters or when you don't care about indexing the column fully.</p><h4>Phone Numbers: VARCHAR, not INT</h4><p>New students often store phone numbers as <code>BIGINT</code>. This is wrong. Phone numbers can have leading zeros (\"080-xxxxxxx\"), plus signs (\"+91-98xxxxxxxx\"), hyphens, and spaces. Storing them as numbers loses all this. Also, nobody does math on phone numbers. <strong>Always store phone numbers as VARCHAR.</strong></p><h3>3. Date and Time Types</h3><table><tr><th>Type</th><th>Format</th><th>Range</th><th>Use</th></tr><tr><td><code>DATE</code></td><td>YYYY-MM-DD</td><td>1000-01-01 to 9999-12-31</td><td>DOB, joining date</td></tr><tr><td><code>TIME</code></td><td>HH:MM:SS</td><td>-838:59:59 to 838:59:59</td><td>Durations, time of day</td></tr><tr><td><code>DATETIME</code></td><td>YYYY-MM-DD HH:MM:SS</td><td>1000-01-01 to 9999-12-31</td><td>Event timestamps</td></tr><tr><td><code>TIMESTAMP</code></td><td>YYYY-MM-DD HH:MM:SS</td><td>1970-01-01 UTC to 2038-01-19 UTC</td><td>Row creation, updates</td></tr><tr><td><code>YEAR</code></td><td>YYYY</td><td>1901 to 2155</td><td>Year-only columns</td></tr></table><h4>DATETIME vs TIMESTAMP — The Difference</h4><p>Both look identical and store \"date + time\". The critical differences:</p><ul><li><code>DATETIME</code> stores the exact literal you give it. No time zone logic. Range is huge (1000 to 9999).</li><li><code>TIMESTAMP</code> is stored internally in UTC and converted to the server's time zone on read. Range is limited: 1970 to 2038 (the Y2K38 problem).</li></ul><p>Use <code>TIMESTAMP</code> for <code>created_at</code> / <code>updated_at</code> columns because it automatically adjusts for server time zones. Use <code>DATETIME</code> for dates that must not shift with time zones — like a scheduled meeting time.</p><h3>4. Boolean</h3><p>MySQL has no true BOOLEAN type. Internally, <code>BOOLEAN</code> is an alias for <code>TINYINT(1)</code> — stored as 0 or 1. <code>TRUE</code> is 1, <code>FALSE</code> is 0. You can write <code>BOOLEAN</code> in your CREATE TABLE for clarity, but MySQL stores it as TINYINT.</p><h3>5. ENUM — Fixed Set of Allowed Values</h3><pre><code>gender ENUM('Male', 'Female', 'Other')\nstatus ENUM('pending', 'approved', 'rejected')</code></pre><p>ENUM restricts the column to one of the listed values. Stored internally as a small integer (1 byte for up to 256 values), so it is also space-efficient. Use it when the set of values is small and rarely changes.</p><h3>6. Constraints</h3><h4>NOT NULL</h4><p>The column cannot be empty. Any INSERT without a value for this column (and no DEFAULT set) fails.</p><pre><code>name VARCHAR(50) NOT NULL</code></pre><h4>UNIQUE</h4><p>All values in the column must be different. Two rows cannot have the same value. NULLs are allowed and multiple NULLs don't count as duplicates in most databases.</p><pre><code>email VARCHAR(100) UNIQUE</code></pre><h4>PRIMARY KEY</h4><p>The column that uniquely identifies each row. Combines UNIQUE + NOT NULL. Only ONE primary key per table. Creates an index automatically.</p><pre><code>student_id INT PRIMARY KEY</code></pre><h4>FOREIGN KEY / REFERENCES</h4><p>Ensures that a column's value exists in another table's primary key. Prevents orphan records.</p><pre><code>student_id INT,\nFOREIGN KEY (student_id) REFERENCES students(student_id)</code></pre><h4>CHECK</h4><p>A boolean condition that every row must satisfy. Supported properly in MySQL 8.0+.</p><pre><code>cgpa DECIMAL(3,2) CHECK (cgpa >= 0 AND cgpa <= 10)</code></pre><h4>DEFAULT</h4><p>Provides a value when no value is specified in INSERT.</p><pre><code>created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP</code></pre><h4>AUTO_INCREMENT</h4><p>Automatically assigns the next integer value to a column. Usually applied to primary key columns.</p><pre><code>id INT AUTO_INCREMENT PRIMARY KEY</code></pre><h3>7. Composite Primary Key</h3><p>Sometimes a single column doesn't uniquely identify a row, but a combination does. Example: a <code>course_enrollments</code> table where one student can enroll in many courses, and one course has many students. Neither <code>student_id</code> alone nor <code>course_id</code> alone is unique, but the pair is.</p><pre><code>CREATE TABLE enrollments (\n    student_id INT,\n    course_id INT,\n    enrolled_on DATE,\n    PRIMARY KEY (student_id, course_id)\n);</code></pre><h3>8. Choosing Data Types — Real Scenarios</h3><table><tr><th>Field</th><th>Good Choice</th><th>Why</th></tr><tr><td>Student age</td><td><code>TINYINT UNSIGNED</code></td><td>Range 0-255 is plenty, 1 byte</td></tr><tr><td>Aadhaar number</td><td><code>CHAR(12)</code></td><td>Exactly 12 digits, keep leading zeros</td></tr><tr><td>Indian phone</td><td><code>VARCHAR(15)</code></td><td>Allows +, spaces, country code</td></tr><tr><td>Profile bio</td><td><code>TEXT</code></td><td>Can be long</td></tr><tr><td>Product price</td><td><code>DECIMAL(10,2)</code></td><td>Exact money</td></tr><tr><td>Date of birth</td><td><code>DATE</code></td><td>No time needed</td></tr><tr><td>Row created timestamp</td><td><code>TIMESTAMP</code></td><td>Auto-converts time zones</td></tr><tr><td>Is active?</td><td><code>BOOLEAN</code></td><td>TINYINT(1) under the hood</td></tr><tr><td>User role (admin/user/guest)</td><td><code>ENUM(...)</code></td><td>Fixed, tiny set</td></tr></table>",
    "codeExamples": [
      {
        "title": "A Well-Designed students Table",
        "code": "CREATE TABLE students (\n    student_id INT AUTO_INCREMENT PRIMARY KEY,\n    name VARCHAR(50) NOT NULL,\n    email VARCHAR(100) NOT NULL UNIQUE,\n    phone VARCHAR(15),\n    dob DATE NOT NULL,\n    cgpa DECIMAL(3,2) DEFAULT 0.00 CHECK (cgpa >= 0 AND cgpa <= 10),\n    is_active BOOLEAN DEFAULT TRUE,\n    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP\n);\n\nDESCRIBE students;",
        "language": "sql",
        "explanation": "This schema uses eight common patterns: <code>AUTO_INCREMENT PRIMARY KEY</code> for a surrogate id; <code>NOT NULL</code> on mandatory fields; <code>UNIQUE</code> on email so no two students share one; phone as <code>VARCHAR(15)</code> to handle +country codes; <code>DECIMAL(3,2)</code> for exact CGPA (range 0.00 to 9.99); <code>CHECK</code> to enforce valid CGPA; <code>BOOLEAN DEFAULT TRUE</code> for active flag; <code>TIMESTAMP DEFAULT CURRENT_TIMESTAMP</code> for automatic creation time.",
        "output": "Query OK, 0 rows affected (0.05 sec)\n\n+------------+---------------+------+-----+-------------------+-------------------+\n| Field      | Type          | Null | Key | Default           | Extra             |\n+------------+---------------+------+-----+-------------------+-------------------+\n| student_id | int           | NO   | PRI | NULL              | auto_increment    |\n| name       | varchar(50)   | NO   |     | NULL              |                   |\n| email      | varchar(100)  | NO   | UNI | NULL              |                   |\n| phone      | varchar(15)   | YES  |     | NULL              |                   |\n| dob        | date          | NO   |     | NULL              |                   |\n| cgpa       | decimal(3,2)  | YES  |     | 0.00              |                   |\n| is_active  | tinyint(1)    | YES  |     | 1                 |                   |\n| created_at | timestamp     | YES  |     | CURRENT_TIMESTAMP | DEFAULT_GENERATED |\n+------------+---------------+------+-----+-------------------+-------------------+\n8 rows in set (0.01 sec)"
      },
      {
        "title": "AUTO_INCREMENT and DEFAULT in Action",
        "code": "-- student_id is AUTO_INCREMENT so we omit it.\n-- is_active and created_at have DEFAULTs so we can omit them too.\nINSERT INTO students (name, email, phone, dob, cgpa) VALUES\n    ('Aarav Sharma', 'aarav@college.edu', '+91-9876543210', '2005-03-15', 8.50),\n    ('Priya Iyer',   'priya@college.edu', '+91-9123456780', '2004-11-22', 9.20),\n    ('Rohan Verma',  'rohan@college.edu', NULL,              '2005-07-08', 7.80);\n\nSELECT student_id, name, email, phone, is_active, created_at FROM students;",
        "language": "sql",
        "explanation": "We name only the columns we want to supply values for. <code>student_id</code> auto-generates 1, 2, 3. <code>is_active</code> defaults to TRUE (1). <code>created_at</code> gets the current timestamp. Rohan's phone is explicitly NULL because phone is nullable (no NOT NULL constraint).",
        "output": "+------------+--------------+-------------------+------------------+-----------+---------------------+\n| student_id | name         | email             | phone            | is_active | created_at          |\n+------------+--------------+-------------------+------------------+-----------+---------------------+\n|          1 | Aarav Sharma | aarav@college.edu | +91-9876543210   |         1 | 2026-04-11 10:15:22 |\n|          2 | Priya Iyer   | priya@college.edu | +91-9123456780   |         1 | 2026-04-11 10:15:22 |\n|          3 | Rohan Verma  | rohan@college.edu | NULL             |         1 | 2026-04-11 10:15:22 |\n+------------+--------------+-------------------+------------------+-----------+---------------------+\n3 rows in set (0.00 sec)"
      },
      {
        "title": "UNIQUE and NOT NULL Rejecting Bad Data",
        "code": "-- Attempt 1: duplicate email — should fail.\nINSERT INTO students (name, email, dob)\nVALUES ('Ananya Reddy', 'aarav@college.edu', '2005-01-10');\n\n-- Attempt 2: NULL name — should fail.\nINSERT INTO students (name, email, dob)\nVALUES (NULL, 'ananya@college.edu', '2005-01-10');\n\n-- Attempt 3: valid row — should succeed.\nINSERT INTO students (name, email, dob)\nVALUES ('Ananya Reddy', 'ananya@college.edu', '2005-01-10');",
        "language": "sql",
        "explanation": "Attempt 1 violates the UNIQUE constraint on email. Attempt 2 violates NOT NULL on name. Attempt 3 obeys both rules and succeeds. This is exactly why constraints exist — the database catches bad data at write time so your reads are always clean.",
        "output": "ERROR 1062 (23000): Duplicate entry 'aarav@college.edu' for key 'students.email'\n\nERROR 1048 (23000): Column 'name' cannot be null\n\nQuery OK, 1 row affected (0.01 sec)"
      },
      {
        "title": "FOREIGN KEY Preventing Orphan Orders",
        "code": "-- orders table references students.\nCREATE TABLE orders (\n    order_id INT AUTO_INCREMENT PRIMARY KEY,\n    student_id INT NOT NULL,\n    amount DECIMAL(10,2) NOT NULL CHECK (amount > 0),\n    order_date DATETIME DEFAULT CURRENT_TIMESTAMP,\n    FOREIGN KEY (student_id) REFERENCES students(student_id)\n);\n\n-- Valid order (student_id=1 exists).\nINSERT INTO orders (student_id, amount) VALUES (1, 1200.50);\n\n-- Invalid order — student_id=999 does not exist.\nINSERT INTO orders (student_id, amount) VALUES (999, 500.00);",
        "language": "sql",
        "explanation": "The FOREIGN KEY clause says \"every student_id in orders must match an existing student_id in students\". Attempt to insert student_id=999 fails because there is no student with id 999. This prevents <strong>orphan records</strong> — orders referencing students who don't exist. The CHECK constraint further ensures amount is positive.",
        "output": "Query OK, 0 rows affected (0.04 sec)\n\nQuery OK, 1 row affected (0.01 sec)\n\nERROR 1452 (23000): Cannot add or update a child row: a foreign key constraint fails (`school`.`orders`, CONSTRAINT `orders_ibfk_1` FOREIGN KEY (`student_id`) REFERENCES `students` (`student_id`))"
      },
      {
        "title": "Composite Primary Key — Enrollments",
        "code": "CREATE TABLE enrollments (\n    student_id INT,\n    course_id INT,\n    enrolled_on DATE DEFAULT (CURRENT_DATE),\n    grade CHAR(2),\n    PRIMARY KEY (student_id, course_id),\n    FOREIGN KEY (student_id) REFERENCES students(student_id)\n);\n\nINSERT INTO enrollments (student_id, course_id, grade) VALUES\n    (1, 101, 'A'),\n    (1, 102, 'B'),\n    (2, 101, 'A'),\n    (2, 102, 'A');\n\n-- Attempt to insert duplicate (1, 101) — fails.\nINSERT INTO enrollments (student_id, course_id, grade) VALUES (1, 101, 'C');",
        "language": "sql",
        "explanation": "A <strong>composite primary key</strong> is made of two or more columns. Here <code>(student_id, course_id)</code> together uniquely identify a row. Student 1 can enroll in multiple courses (101 and 102), and course 101 can have multiple students (1 and 2), but each (student, course) pair can appear at most once. The last INSERT fails because (1, 101) already exists.",
        "output": "Query OK, 0 rows affected (0.03 sec)\n\nQuery OK, 4 rows affected (0.01 sec)\nRecords: 4  Duplicates: 0  Warnings: 0\n\nERROR 1062 (23000): Duplicate entry '1-101' for key 'enrollments.PRIMARY'"
      },
      {
        "title": "DECIMAL vs FLOAT — Why Money Needs DECIMAL",
        "code": "CREATE TABLE payments_decimal (amount DECIMAL(10,2));\nCREATE TABLE payments_float (amount FLOAT);\n\n-- Insert 0.1 three times into each.\nINSERT INTO payments_decimal VALUES (0.10), (0.10), (0.10);\nINSERT INTO payments_float VALUES (0.10), (0.10), (0.10);\n\n-- Sum them.\nSELECT SUM(amount) AS total_decimal FROM payments_decimal;\nSELECT SUM(amount) AS total_float FROM payments_float;",
        "language": "sql",
        "explanation": "With DECIMAL the sum is exactly 0.30 — what any accountant expects. With FLOAT the sum is 0.30000000447... due to binary floating-point rounding errors. On a real ledger with millions of transactions, these tiny errors add up and cause balance mismatches. <strong>Always use DECIMAL for money.</strong>",
        "output": "+---------------+\n| total_decimal |\n+---------------+\n|          0.30 |\n+---------------+\n1 row in set (0.00 sec)\n\n+----------------------+\n| total_float          |\n+----------------------+\n| 0.30000001192092896  |\n+----------------------+\n1 row in set (0.00 sec)"
      }
    ],
    "commonMistakes": [
      {
        "title": "Storing Phone Numbers as INT",
        "wrongCode": "CREATE TABLE users (\n    id INT,\n    phone INT   -- WRONG\n);\nINSERT INTO users VALUES (1, 09876543210);",
        "error": "Leading zero is lost. '09876543210' becomes 9876543210. International numbers with '+' or '-' cannot be stored at all.",
        "correctCode": "CREATE TABLE users (\n    id INT PRIMARY KEY,\n    phone VARCHAR(15)\n);\nINSERT INTO users VALUES (1, '+91-9876543210');",
        "explanation": "Phone numbers are not numbers in the arithmetic sense — you never add two phone numbers. They can have leading zeros, country codes, plus signs, spaces, hyphens. Store them as VARCHAR(15) or VARCHAR(20). The same rule applies to Aadhaar numbers, PIN codes with leading zeros, and credit card numbers."
      },
      {
        "title": "Using FLOAT for Money",
        "wrongCode": "CREATE TABLE orders (\n    order_id INT,\n    amount FLOAT  -- WRONG\n);\nINSERT INTO orders VALUES (1, 19.99), (2, 10.01);\nSELECT SUM(amount) FROM orders;  -- returns 30.000000190734863",
        "error": "Floating-point cannot exactly represent 19.99, so sums drift over many rows.",
        "correctCode": "CREATE TABLE orders (\n    order_id INT,\n    amount DECIMAL(10,2)\n);\nINSERT INTO orders VALUES (1, 19.99), (2, 10.01);\nSELECT SUM(amount) FROM orders;  -- returns 30.00",
        "explanation": "DECIMAL stores the exact digits you give it. FLOAT and DOUBLE store an approximation in binary, which loses precision on values like 0.1, 0.2, 0.3. For anything touching money (prices, taxes, invoices, salaries), always use DECIMAL with enough precision."
      },
      {
        "title": "Forgetting NOT NULL on Required Fields",
        "wrongCode": "CREATE TABLE students (\n    id INT PRIMARY KEY,\n    name VARCHAR(50),\n    email VARCHAR(100)\n);\nINSERT INTO students (id) VALUES (1);  -- succeeds but creates a student with no name or email",
        "error": "Bad data silently gets in. Reports later show students with NULL names.",
        "correctCode": "CREATE TABLE students (\n    id INT PRIMARY KEY,\n    name VARCHAR(50) NOT NULL,\n    email VARCHAR(100) NOT NULL UNIQUE\n);\nINSERT INTO students (id) VALUES (1);  -- now fails at INSERT time",
        "explanation": "If a field is conceptually required, declare it NOT NULL. This catches bugs at insert time (a controlled failure) rather than at report time weeks later (a confused stakeholder). Combine NOT NULL with UNIQUE for fields like email, username, or pan_number that must always exist AND be unique."
      },
      {
        "title": "Thinking Two UNIQUE Constraints Are Equivalent to a Composite PRIMARY KEY",
        "wrongCode": "CREATE TABLE enrollments (\n    student_id INT UNIQUE,\n    course_id INT UNIQUE,\n    enrolled_on DATE\n);",
        "error": "UNIQUE on student_id alone means each student can appear only once — breaks the use case (a student takes many courses).",
        "correctCode": "CREATE TABLE enrollments (\n    student_id INT,\n    course_id INT,\n    enrolled_on DATE,\n    PRIMARY KEY (student_id, course_id)\n);",
        "explanation": "A UNIQUE on a single column means that column is unique by itself. UNIQUE on student_id AND UNIQUE on course_id means each student can appear only once AND each course can appear only once — not what we want. A <strong>composite primary key on (student_id, course_id)</strong> means the <em>pair</em> is unique, which is exactly the real-world rule."
      }
    ],
    "summary": [
      "Data types control what kind of values a column can hold. Constraints add business rules on top of the types.",
      "Numeric types: TINYINT (1 byte), INT (4 bytes, most common), BIGINT (8 bytes). Use DECIMAL(p, s) for money, never FLOAT or DOUBLE.",
      "String types: CHAR(n) for fixed-length (codes, hashes); VARCHAR(n) for variable-length (names, emails); TEXT for long free-form text.",
      "Phone numbers, Aadhaar, and PINs should be VARCHAR (not INT) because leading zeros, plus signs, and punctuation matter.",
      "Date/time types: DATE (YYYY-MM-DD), DATETIME (up to year 9999), TIMESTAMP (stored as UTC, Y2K38 limit in 2038).",
      "Use TIMESTAMP for created_at/updated_at (auto time-zone). Use DATETIME for fixed wall-clock times that must never shift.",
      "MySQL BOOLEAN is internally TINYINT(1): TRUE=1, FALSE=0. ENUM restricts a column to a fixed set of allowed values.",
      "Constraints: NOT NULL (required), UNIQUE (no duplicates), PRIMARY KEY (= NOT NULL + UNIQUE, one per table), FOREIGN KEY (references another table), CHECK (boolean rule), DEFAULT (fallback value), AUTO_INCREMENT (auto-assigned ids).",
      "A composite primary key is made of multiple columns; the combination must be unique. Used for junction tables like enrollments.",
      "Good schema = right types + right constraints. Both together turn your database into a fortress that rejects bad data at the source, saving you from painful data cleanups later."
    ]
  },
  "relatedTopics": ["setting-up-mysql-and-first-query", "creating-and-modifying-tables"],
  "practice": {
    "totalQuestions": 56,
    "topicSpecific": [
      {
        "id": "ts-1",
        "type": "conceptual",
        "difficulty": "Easy",
        "question": "What data type should you use for a student's age?",
        "hint": "Range is 0-150 at most.",
        "answer": "<code>TINYINT UNSIGNED</code> (range 0-255, 1 byte) is ideal. <code>INT</code> works but wastes 3 bytes per row.",
        "explanation": "TINYINT UNSIGNED is perfect because ages never go negative and never exceed 255. On a million-row table, picking TINYINT over INT saves 3MB — small but it adds up across columns."
      },
      {
        "id": "ts-2",
        "type": "conceptual",
        "difficulty": "Easy",
        "question": "What data type for a 12-digit Aadhaar number, and why not BIGINT?",
        "hint": "Aadhaar can start with 0, and you never do math on it.",
        "answer": "<code>CHAR(12)</code>. Not BIGINT because (a) Aadhaar numbers can start with 0 which BIGINT would strip, and (b) we never perform arithmetic on an Aadhaar.",
        "explanation": "CHAR(12) is better than VARCHAR(12) because every valid Aadhaar is exactly 12 digits — the fixed-length CHAR gives slightly better performance and signals intent. Also consider adding a CHECK constraint to ensure all 12 characters are digits."
      },
      {
        "id": "ts-3",
        "type": "conceptual",
        "difficulty": "Easy",
        "question": "What data type for a user's profile bio that can be up to 2000 characters?",
        "hint": "Varchar limit vs TEXT.",
        "answer": "<code>VARCHAR(2000)</code> or <code>TEXT</code>. VARCHAR is preferred here since 2000 chars is well below the VARCHAR limit and VARCHAR can be indexed fully.",
        "explanation": "TEXT starts to matter only beyond 65,535 characters. At 2000 characters, VARCHAR(2000) is simpler and allows full indexing if you want to search bios later. Only use TEXT for truly long content (articles, blog posts, logs)."
      },
      {
        "id": "ts-4",
        "type": "conceptual",
        "difficulty": "Easy",
        "question": "What data type for a date of birth?",
        "hint": "Do you need the time of day?",
        "answer": "<code>DATE</code>. Only year-month-day is needed — not hours or minutes.",
        "explanation": "Using DATETIME for DOB wastes space (3 extra bytes per row) and invites bugs where someone accidentally uses the time part. Use DATE for all date-only fields: joining_date, dob, appointment_date."
      },
      {
        "id": "ts-5",
        "type": "conceptual",
        "difficulty": "Easy",
        "question": "What data type for a product price like 1499.50?",
        "hint": "Never FLOAT for money.",
        "answer": "<code>DECIMAL(10, 2)</code> — up to 10 total digits with 2 after the decimal. Supports up to 99,999,999.99.",
        "explanation": "DECIMAL stores exact values, which is essential for money. FLOAT would introduce rounding errors that make account balances drift. Pick the first number (precision) large enough for your max amount, and 2 for paise/cents."
      },
      {
        "id": "ts-6",
        "type": "conceptual",
        "difficulty": "Easy",
        "question": "Name three constraints that can be applied to a column in SQL.",
        "hint": "Any three of six common ones.",
        "answer": "Any three of: <code>NOT NULL</code>, <code>UNIQUE</code>, <code>PRIMARY KEY</code>, <code>FOREIGN KEY</code>, <code>CHECK</code>, <code>DEFAULT</code>.",
        "explanation": "These are the six standard column-level constraints. AUTO_INCREMENT is technically a column attribute, not a constraint, but interviewers often accept it too. Each constraint protects your data against a different class of bugs."
      },
      {
        "id": "ts-7",
        "type": "conceptual",
        "difficulty": "Easy",
        "question": "What is the difference between PRIMARY KEY and UNIQUE?",
        "hint": "Nullability and count per table.",
        "answer": "<strong>PRIMARY KEY</strong>: uniquely identifies a row, cannot be NULL, only ONE per table. <strong>UNIQUE</strong>: values must be different, CAN be NULL (and multiple NULLs are usually allowed), MANY UNIQUE constraints allowed per table.",
        "explanation": "Think of PRIMARY KEY as the main identity card and UNIQUE as additional optional constraints. A student's primary key might be student_id, but email and aadhaar can each have their own UNIQUE constraint to prevent duplicates."
      },
      {
        "id": "ts-8",
        "type": "conceptual",
        "difficulty": "Easy",
        "question": "What does AUTO_INCREMENT do?",
        "hint": "Relates to ID generation.",
        "answer": "<code>AUTO_INCREMENT</code> automatically assigns the next integer value to a column on each INSERT, so you don't have to provide the id yourself.",
        "explanation": "Usually applied to the primary key: <code>id INT AUTO_INCREMENT PRIMARY KEY</code>. Start value defaults to 1 and increments by 1. If you delete row with id=5 and insert a new row, it becomes id=6 (not 5) — gaps are normal."
      },
      {
        "id": "ts-9",
        "type": "conceptual",
        "difficulty": "Easy",
        "question": "What is the difference between CHAR(10) and VARCHAR(10)?",
        "hint": "Storage and padding.",
        "answer": "<code>CHAR(10)</code> always takes exactly 10 characters of storage (padded with spaces if shorter). <code>VARCHAR(10)</code> takes only the actual length plus 1-2 bytes overhead. VARCHAR is better for variable-length data; CHAR is marginally faster for strictly fixed-length data.",
        "explanation": "If you store \"IN\" in CHAR(10), MySQL pads it to \"IN        \" on disk. If you store it in VARCHAR(10), it takes 2 bytes + 1 byte of length prefix = 3 bytes. For fixed codes (country=IN, US), CHAR is cleaner. For names, emails, addresses, VARCHAR wins."
      },
      {
        "id": "ts-10",
        "type": "conceptual",
        "difficulty": "Medium",
        "question": "When would you use DATETIME vs TIMESTAMP?",
        "hint": "Time zones and range.",
        "answer": "<strong>DATETIME</strong>: stores literal wall-clock time, range 1000-9999, no time zone logic. Use for fixed events like scheduled meeting times. <strong>TIMESTAMP</strong>: stored as UTC, converted to server time zone on read, range 1970-2038. Use for created_at/updated_at where automatic time zone handling is helpful.",
        "explanation": "Rule of thumb: TIMESTAMP for \"when did the system record this event\" (auto-updates with session time zone), DATETIME for \"what wall-clock moment is the event\" (same value no matter where you read from)."
      },
      {
        "id": "ts-11",
        "type": "conceptual",
        "difficulty": "Medium",
        "question": "What is a composite primary key? Give an example.",
        "hint": "More than one column.",
        "answer": "A composite primary key is made of two or more columns. The COMBINATION must be unique, though each individual column may have duplicates. Example: an enrollments(student_id, course_id) table where a student can take many courses and a course has many students, but each (student, course) pair is unique.",
        "explanation": "Composite PKs are standard for junction tables that resolve many-to-many relationships. Syntax: <code>PRIMARY KEY (col1, col2)</code> at the end of CREATE TABLE."
      },
      {
        "id": "ts-12",
        "type": "conceptual",
        "difficulty": "Medium",
        "question": "Why should you never store money as FLOAT?",
        "hint": "Binary representation of decimal fractions.",
        "answer": "FLOAT stores values in binary floating-point, which cannot exactly represent decimal fractions like 0.1 or 19.99. Summing 0.1 ten times gives 0.9999999 instead of 1.0. Over millions of transactions, balances silently drift. <strong>Always use DECIMAL for money.</strong>",
        "explanation": "A real-world horror story: a startup's billing system used FLOAT for transactions; after a year, the total reported by their DB disagreed with the total reported by their payment provider by hundreds of rupees. Fixing it required re-processing months of data."
      },
      {
        "id": "ts-13",
        "type": "coding",
        "difficulty": "Medium",
        "question": "Write a CREATE TABLE for <code>employees</code> with: emp_id (auto-increment primary key), name (required up to 50 chars), email (unique, required), salary (decimal for money, required, must be positive), hire_date (date, required, default today).",
        "hint": "Combine all the constraints you learned.",
        "answer": "<pre><code>CREATE TABLE employees (\n    emp_id INT AUTO_INCREMENT PRIMARY KEY,\n    name VARCHAR(50) NOT NULL,\n    email VARCHAR(100) NOT NULL UNIQUE,\n    salary DECIMAL(10,2) NOT NULL CHECK (salary &gt; 0),\n    hire_date DATE NOT NULL DEFAULT (CURRENT_DATE)\n);</code></pre>",
        "explanation": "Combines AUTO_INCREMENT, PRIMARY KEY, NOT NULL, UNIQUE, CHECK, and DEFAULT. <code>CURRENT_DATE</code> as the default for hire_date means if you INSERT without a hire_date, MySQL fills in today's date."
      },
      {
        "id": "ts-14",
        "type": "coding",
        "difficulty": "Medium",
        "question": "Write a CREATE TABLE for an <code>orders</code> table that links to employees via a FOREIGN KEY.",
        "hint": "FOREIGN KEY ... REFERENCES.",
        "answer": "<pre><code>CREATE TABLE orders (\n    order_id INT AUTO_INCREMENT PRIMARY KEY,\n    emp_id INT NOT NULL,\n    amount DECIMAL(10,2) NOT NULL CHECK (amount &gt; 0),\n    order_date DATETIME DEFAULT CURRENT_TIMESTAMP,\n    FOREIGN KEY (emp_id) REFERENCES employees(emp_id)\n);</code></pre>",
        "explanation": "The FOREIGN KEY clause ensures every emp_id in orders points to a real row in employees. Attempting to insert an order with a non-existent emp_id fails immediately. This is the foundation of relational integrity."
      },
      {
        "id": "ts-15",
        "type": "coding",
        "difficulty": "Medium",
        "question": "Write a CREATE TABLE for a junction table <code>enrollments</code> that captures which student is enrolled in which course, with enrollment date. Use a composite primary key.",
        "hint": "PRIMARY KEY (student_id, course_id).",
        "answer": "<pre><code>CREATE TABLE enrollments (\n    student_id INT,\n    course_id INT,\n    enrolled_on DATE DEFAULT (CURRENT_DATE),\n    PRIMARY KEY (student_id, course_id)\n);</code></pre>",
        "explanation": "Composite PK ensures no duplicate (student, course) pairs. Without it, a student could accidentally be enrolled in the same course twice. You would typically also add two FOREIGN KEYs to students and courses."
      },
      {
        "id": "ts-16",
        "type": "conceptual",
        "difficulty": "Medium",
        "question": "Can a PRIMARY KEY column be NULL?",
        "hint": "Think about what a primary key represents.",
        "answer": "No. A PRIMARY KEY is implicitly NOT NULL. It must uniquely identify each row, and NULL means \"unknown\", which cannot uniquely identify anything.",
        "explanation": "Even if you don't write NOT NULL, MySQL adds it for you when you declare PRIMARY KEY. If you try to INSERT a row with NULL in the PK, you get an error: \"Column 'id' cannot be null\"."
      },
      {
        "id": "ts-17",
        "type": "conceptual",
        "difficulty": "Medium",
        "question": "Can a UNIQUE column be NULL?",
        "hint": "Yes, usually, and multiple NULLs are allowed.",
        "answer": "Yes. In MySQL, a UNIQUE column allows NULL values and treats multiple NULLs as NOT duplicates of each other — so multiple rows can have NULL in a UNIQUE column.",
        "explanation": "This is a subtle interview question. Example: a users table with <code>referral_code VARCHAR(20) UNIQUE</code>. Many users may have NULL referral_code (they weren't referred by anyone). That's fine; only non-null values must be distinct. PostgreSQL and Oracle behave similarly, but SQL Server historically treats a single NULL as unique (allowing at most one NULL)."
      },
      {
        "id": "ts-18",
        "type": "conceptual",
        "difficulty": "Hard",
        "question": "Your team wants to add a CHECK constraint to ensure an age column is between 18 and 100. Write the constraint.",
        "hint": "CHECK (condition).",
        "answer": "<pre><code>age INT NOT NULL CHECK (age BETWEEN 18 AND 100)</code></pre>Or equivalently: <code>CHECK (age &gt;= 18 AND age &lt;= 100)</code>.",
        "explanation": "CHECK constraints enforce arbitrary boolean conditions on each row. MySQL 8.0+ enforces them properly (older MySQL silently ignored them). BETWEEN is inclusive, so 18 and 100 themselves are allowed."
      },
      {
        "id": "ts-19",
        "type": "conceptual",
        "difficulty": "Hard",
        "question": "Why might you use ENUM('Male', 'Female', 'Other') instead of VARCHAR(10) for a gender column?",
        "hint": "Space and validation.",
        "answer": "Two reasons: (1) ENUM is stored internally as a 1-byte integer regardless of string length — far more efficient; (2) ENUM restricts values to the listed options, so no one can insert 'male ' (trailing space), 'MALE', or 'foo' — the database validates for you.",
        "explanation": "Downside of ENUM: adding a new option requires ALTER TABLE (not free on huge tables). For columns with stable, well-known options (gender, order_status, priority_level), ENUM is concise and self-documenting. For columns that might evolve often, VARCHAR with a lookup table is more flexible."
      },
      {
        "id": "ts-20",
        "type": "conceptual",
        "difficulty": "Hard",
        "question": "Your DBA proposes using DOUBLE for storing student CGPAs. Is that acceptable or would you push back?",
        "hint": "Precision matters for grades.",
        "answer": "Push back. CGPA is shown to students to 2 decimal places and can cause disputes if rounding drifts (9.24 vs 9.25 affects scholarships). Use <code>DECIMAL(3, 2)</code> for exact 0.00-9.99 representation.",
        "explanation": "DOUBLE is fine for science-lab data where small floating-point errors don't matter. For anything students, HR, or auditors might dispute (grades, marks, salaries, money), use DECIMAL. Your future self doing end-of-year reconciliation will thank you."
      }
    ],
    "mixed": [
      {
        "id": "mx-1",
        "type": "conceptual",
        "difficulty": "Easy",
        "question": "Match each data type with its best use case: INT, VARCHAR(50), DATE, DECIMAL(10,2), BOOLEAN.",
        "hint": "One per column.",
        "answer": "INT → row IDs, counts. VARCHAR(50) → names, emails. DATE → date of birth, joining date. DECIMAL(10,2) → money, product price. BOOLEAN → yes/no flags like is_active.",
        "explanation": "This mental mapping is one of the most useful tools for designing schemas quickly. Knowing the right type for each conceptual shape of data takes you 80% of the way to a good schema."
      },
      {
        "id": "mx-2",
        "type": "debugging",
        "difficulty": "Easy",
        "question": "Spot the bad choice and fix it:<br><pre><code>CREATE TABLE users (\n    id INT,\n    phone INT,\n    bio VARCHAR(5000),\n    balance FLOAT\n);</code></pre>",
        "hint": "Phone, bio, balance all wrong.",
        "answer": "<pre><code>CREATE TABLE users (\n    id INT PRIMARY KEY,\n    phone VARCHAR(15),       -- was INT, lost leading zeros\n    bio TEXT,                 -- was VARCHAR(5000), too big for VARCHAR\n    balance DECIMAL(12,2)     -- was FLOAT, loses precision on money\n);</code></pre>",
        "explanation": "Three classic mistakes in one: phone as INT (loses leading zeros), bio as VARCHAR(5000) (works but TEXT is more appropriate for long free text), and balance as FLOAT (rounding errors on money). Also added PRIMARY KEY on id for good measure."
      },
      {
        "id": "mx-3",
        "type": "conceptual",
        "difficulty": "Medium",
        "question": "Can a table have zero constraints? Is that a good design?",
        "hint": "MySQL allows it, but...",
        "answer": "Yes, MySQL allows it. But it is a poor design. Without constraints, bad data silently flows in and you end up cleaning the mess later in queries.",
        "explanation": "Every production table should have at least a PRIMARY KEY. Most need NOT NULL on required fields and UNIQUE on identity-like fields (email, username). FOREIGN KEYs are important for maintaining referential integrity in related tables."
      },
      {
        "id": "mx-4",
        "type": "coding",
        "difficulty": "Medium",
        "question": "Create a <code>products</code> table: id auto-increment PK, name required up to 80 chars, sku unique required 20 chars, price decimal for money must be positive, category an ENUM of 'Electronics', 'Books', 'Clothing', and is_active boolean defaulting TRUE.",
        "hint": "Combine everything.",
        "answer": "<pre><code>CREATE TABLE products (\n    id INT AUTO_INCREMENT PRIMARY KEY,\n    name VARCHAR(80) NOT NULL,\n    sku VARCHAR(20) NOT NULL UNIQUE,\n    price DECIMAL(10,2) NOT NULL CHECK (price &gt; 0),\n    category ENUM('Electronics', 'Books', 'Clothing') NOT NULL,\n    is_active BOOLEAN DEFAULT TRUE\n);</code></pre>",
        "explanation": "This is a complete, realistic product catalog schema. Every column is typed correctly and constrained to prevent bad data. This is what a senior reviewer would expect from a junior engineer."
      },
      {
        "id": "mx-5",
        "type": "conceptual",
        "difficulty": "Medium",
        "question": "What happens if you try to insert a CGPA of 11.5 into a column declared as DECIMAL(3,2) CHECK (cgpa &lt;= 10)?",
        "hint": "Two things could fail.",
        "answer": "Two possible issues. First, DECIMAL(3,2) means 3 total digits with 2 after the decimal — max allowed is 9.99, so 11.5 exceeds the type's max. Second, even if the type allowed it, the CHECK (cgpa &lt;= 10) would reject it. In MySQL you'll get an \"Out of range\" error first.",
        "explanation": "DECIMAL(p, s) means p = total digits, s = digits after decimal. So DECIMAL(3,2) allows at most 9.99. For CGPA on a 10-point scale, use DECIMAL(4,2) to safely hold 10.00, plus a CHECK to enforce the business rule."
      },
      {
        "id": "mx-6",
        "type": "conceptual",
        "difficulty": "Medium",
        "question": "Why do FOREIGN KEY constraints sometimes prevent deletes you think should work?",
        "hint": "Orphan child rows.",
        "answer": "If another table references a row via FOREIGN KEY, deleting the parent leaves orphan children. MySQL blocks the delete by default. You must either delete the children first or configure ON DELETE CASCADE / SET NULL on the FK.",
        "explanation": "Example: students has FK to departments. If you try to delete a department that has students, MySQL errors: \"Cannot delete or update a parent row: a foreign key constraint fails\". Fix: delete/reassign the students first, or declare the FK with <code>ON DELETE CASCADE</code> (auto-delete children) or <code>ON DELETE SET NULL</code> (orphan children but keep them)."
      },
      {
        "id": "mx-7",
        "type": "conceptual",
        "difficulty": "Hard",
        "question": "A colleague declares a column as <code>VARCHAR(50)</code> but always inserts values exactly 50 characters long. Is VARCHAR fine here or should they switch to CHAR?",
        "hint": "Fixed length implies fixed storage.",
        "answer": "Switch to <code>CHAR(50)</code>. Since every value is exactly 50 characters, CHAR is slightly faster (fixed-width rows) and saves the 1-2 byte length prefix that VARCHAR adds to every value.",
        "explanation": "Micro-optimization? Yes, but it matters on tables with 100M rows. CHAR also subtly documents intent: \"this value is always exactly 50 chars\". Common example: MD5 hashes (always 32 chars) → CHAR(32)."
      },
      {
        "id": "mx-8",
        "type": "conceptual",
        "difficulty": "Hard",
        "question": "You need to store the exact moment (to the microsecond) a user clicked a button. Which type: DATETIME(6), TIMESTAMP(6), or BIGINT storing epoch microseconds?",
        "hint": "Time zone and range matter.",
        "answer": "Depends. <strong>TIMESTAMP(6)</strong> gives microsecond precision and automatic UTC handling — best if you are before 2038. <strong>DATETIME(6)</strong> gives microseconds but no time zone logic — best if you might store events beyond 2038 or want the literal wall-clock time. <strong>BIGINT</strong> is the most portable and survives the 2038 bug but requires application-level conversion to a readable date.",
        "explanation": "Most modern web apps use TIMESTAMP(6) for click events and analytics (current times, auto TZ conversion). Systems that must work past 2038 use BIGINT for safety. Remember: both TIMESTAMP and DATETIME support fractional seconds only if you specify the precision, e.g., <code>DATETIME(6)</code>."
      },
      {
        "id": "mx-9",
        "type": "conceptual",
        "difficulty": "Hard",
        "question": "Your friend says: \"Since UNIQUE constraints prevent duplicates, we can just slap UNIQUE on many columns and skip the PRIMARY KEY.\" Counter this.",
        "hint": "Primary keys do more than uniqueness.",
        "answer": "Three reasons to still have a PRIMARY KEY: (1) PK implies NOT NULL (UNIQUE alone allows NULL). (2) PK creates the main clustered index in InnoDB — hugely impacts storage layout and query speed. (3) Foreign keys from other tables need a primary key (or unique key) to reference.",
        "explanation": "A table without a PRIMARY KEY is technically legal in MySQL but strongly discouraged. InnoDB will invent a hidden 6-byte row ID internally anyway. Being explicit about the PK makes the schema readable and makes joins/indexes work predictably."
      },
      {
        "id": "mx-10",
        "type": "debugging",
        "difficulty": "Hard",
        "question": "Fix the design error:<br><pre><code>CREATE TABLE bookings (\n    user_id INT UNIQUE,\n    room_id INT UNIQUE,\n    booking_date DATE\n);</code></pre>The business rule is: a user can book many rooms; a room can be booked by many users; but no (user, room, date) triple should repeat.",
        "hint": "Neither column alone is unique.",
        "answer": "<pre><code>CREATE TABLE bookings (\n    user_id INT,\n    room_id INT,\n    booking_date DATE,\n    PRIMARY KEY (user_id, room_id, booking_date)\n);</code></pre>",
        "explanation": "The bad version's UNIQUE constraints mean a user can book only one room ever, and a room is booked by only one user ever — wrong. The fix is a composite primary key on the three columns together, ensuring the TRIPLE is unique but each column individually can repeat."
      },
      {
        "id": "mx-11",
        "type": "conceptual",
        "difficulty": "Hard",
        "question": "A table has <code>created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP</code> and <code>updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP</code>. Explain what happens on INSERT and on UPDATE.",
        "hint": "ON UPDATE changes the behavior.",
        "answer": "On INSERT: both columns get the current timestamp. On UPDATE of any other column in the row: <code>updated_at</code> is automatically refreshed to the current timestamp, but <code>created_at</code> stays unchanged.",
        "explanation": "This is the standard \"auditing columns\" pattern used in virtually every production schema. created_at tells you when a row was born; updated_at tells you when it was last modified. Without the <code>ON UPDATE CURRENT_TIMESTAMP</code> clause, updated_at would stay at its insert value forever."
      },
      {
        "id": "mx-12",
        "type": "coding",
        "difficulty": "Hard",
        "question": "Design a <code>course_enrollments</code> table for a college. Rules: a student takes many courses; a course has many students; each enrollment has a date and optional final grade (A, B, C, D, F); you should not be able to re-enroll the same student in the same course (unless they're repeating, in which case a separate attempt_no distinguishes them).",
        "hint": "Composite PK with attempt_no.",
        "answer": "<pre><code>CREATE TABLE course_enrollments (\n    student_id INT NOT NULL,\n    course_id INT NOT NULL,\n    attempt_no TINYINT NOT NULL DEFAULT 1,\n    enrolled_on DATE NOT NULL DEFAULT (CURRENT_DATE),\n    grade CHAR(1) CHECK (grade IN ('A','B','C','D','F')),\n    PRIMARY KEY (student_id, course_id, attempt_no),\n    FOREIGN KEY (student_id) REFERENCES students(student_id),\n    FOREIGN KEY (course_id) REFERENCES courses(course_id)\n);</code></pre>",
        "explanation": "The composite PK on (student_id, course_id, attempt_no) lets a student retake the same course with a new attempt_no. FKs to students and courses prevent orphan records. CHECK on grade restricts it to valid letter grades (and NULL, meaning not yet graded)."
      }
    ],
    "mcqs": [
      {
        "id": "mcq-1",
        "difficulty": "Easy",
        "question": "Which data type is best for storing exact monetary amounts?",
        "options": {
          "A": "FLOAT",
          "B": "DOUBLE",
          "C": "DECIMAL",
          "D": "INT"
        },
        "correct": "C",
        "explanation": "<strong>C is correct.</strong> DECIMAL stores values exactly, which is essential for money. FLOAT and DOUBLE introduce binary floating-point rounding errors. INT can't store paise/cents as a decimal."
      },
      {
        "id": "mcq-2",
        "difficulty": "Easy",
        "question": "Which data type should a 10-digit Indian mobile number be stored as?",
        "options": {
          "A": "INT",
          "B": "BIGINT",
          "C": "DECIMAL(10,0)",
          "D": "VARCHAR(15)"
        },
        "correct": "D",
        "explanation": "<strong>D is correct.</strong> Phone numbers may have leading zeros, country codes, hyphens, or spaces. VARCHAR preserves all of that. Numeric types would strip leading zeros and block international formats."
      },
      {
        "id": "mcq-3",
        "difficulty": "Easy",
        "question": "How many primary keys can a table have?",
        "options": {
          "A": "One",
          "B": "Two",
          "C": "As many as you want",
          "D": "Zero"
        },
        "correct": "A",
        "explanation": "<strong>A is correct.</strong> A table can have only ONE primary key (though it can be composed of multiple columns — a composite key). You can have many UNIQUE constraints, but only one PRIMARY KEY."
      },
      {
        "id": "mcq-4",
        "difficulty": "Easy",
        "question": "Which keyword enforces that a column cannot have duplicate values?",
        "options": {
          "A": "NOT NULL",
          "B": "UNIQUE",
          "C": "CHECK",
          "D": "DEFAULT"
        },
        "correct": "B",
        "explanation": "<strong>B is correct.</strong> UNIQUE prevents duplicate values. NOT NULL prevents empty values. CHECK enforces a general condition. DEFAULT provides a fallback."
      },
      {
        "id": "mcq-5",
        "difficulty": "Easy",
        "question": "Which data type stores only date (no time) in MySQL?",
        "options": {
          "A": "DATETIME",
          "B": "TIMESTAMP",
          "C": "DATE",
          "D": "TIME"
        },
        "correct": "C",
        "explanation": "<strong>C is correct.</strong> DATE stores YYYY-MM-DD only. DATETIME and TIMESTAMP include time. TIME stores only HH:MM:SS."
      },
      {
        "id": "mcq-6",
        "difficulty": "Easy",
        "question": "What does AUTO_INCREMENT do?",
        "options": {
          "A": "Automatically updates the column on every change",
          "B": "Automatically generates a new integer value for each inserted row",
          "C": "Automatically deletes old rows",
          "D": "Automatically checks constraints"
        },
        "correct": "B",
        "explanation": "<strong>B is correct.</strong> AUTO_INCREMENT generates a new (usually sequential) integer for each INSERT. You can omit the column in INSERT and MySQL fills it in."
      },
      {
        "id": "mcq-7",
        "difficulty": "Easy",
        "question": "Which type is appropriate for an Aadhaar number (12 digits, fixed length)?",
        "options": {
          "A": "INT",
          "B": "BIGINT",
          "C": "CHAR(12)",
          "D": "VARCHAR(12)"
        },
        "correct": "C",
        "explanation": "<strong>C is correct.</strong> Aadhaar is exactly 12 digits, never changes length, and can have leading zeros. CHAR(12) is slightly better than VARCHAR(12) for fixed-length strings. Numeric types lose leading zeros."
      },
      {
        "id": "mcq-8",
        "difficulty": "Medium",
        "question": "Which combination of constraints does PRIMARY KEY imply?",
        "options": {
          "A": "UNIQUE only",
          "B": "NOT NULL only",
          "C": "UNIQUE and NOT NULL",
          "D": "CHECK and DEFAULT"
        },
        "correct": "C",
        "explanation": "<strong>C is correct.</strong> A PRIMARY KEY is implicitly both UNIQUE (no duplicate values) and NOT NULL (must have a value). That's why it can uniquely identify every row."
      },
      {
        "id": "mcq-9",
        "difficulty": "Medium",
        "question": "What is the difference between DATETIME and TIMESTAMP in MySQL?",
        "options": {
          "A": "TIMESTAMP is time-zone-aware and has a smaller range (1970-2038); DATETIME is literal and has a huge range (1000-9999)",
          "B": "They are the same thing",
          "C": "TIMESTAMP stores only the time, DATETIME stores only the date",
          "D": "DATETIME is faster than TIMESTAMP"
        },
        "correct": "A",
        "explanation": "<strong>A is correct.</strong> TIMESTAMP is stored in UTC and auto-converted to session time zone on read, but its range is limited due to the Y2K38 problem. DATETIME is stored as the literal wall-clock time with a 1000-9999 range, no TZ logic."
      },
      {
        "id": "mcq-10",
        "difficulty": "Medium",
        "question": "Which is a correct CREATE TABLE statement?",
        "options": {
          "A": "CREATE TABLE t (a INT UNIQUE, b INT UNIQUE, PRIMARY KEY (a, b));",
          "B": "CREATE TABLE t (a INT PRIMARY KEY PRIMARY KEY);",
          "C": "CREATE TABLE t (a INT, b INT, PRIMARY KEY a);",
          "D": "CREATE TABLE t (a INT, b INT, PRIMARY KEY (a), PRIMARY KEY (b));"
        },
        "correct": "A",
        "explanation": "<strong>A is correct.</strong> You can have multiple UNIQUE columns plus a composite PRIMARY KEY. B has duplicate keywords. C has syntax error (no parentheses around column name). D has two PRIMARY KEYs which isn't allowed."
      },
      {
        "id": "mcq-11",
        "difficulty": "Medium",
        "question": "Which of the following is NOT a valid MySQL numeric type?",
        "options": {
          "A": "TINYINT",
          "B": "SMALLINT",
          "C": "LARGEINT",
          "D": "BIGINT"
        },
        "correct": "C",
        "explanation": "<strong>C is correct.</strong> LARGEINT is not a MySQL type. The valid integer types are TINYINT (1 byte), SMALLINT (2), MEDIUMINT (3), INT (4), and BIGINT (8)."
      },
      {
        "id": "mcq-12",
        "difficulty": "Medium",
        "question": "In MySQL, BOOLEAN is internally stored as which type?",
        "options": {
          "A": "CHAR(1)",
          "B": "TINYINT(1)",
          "C": "BIT",
          "D": "ENUM"
        },
        "correct": "B",
        "explanation": "<strong>B is correct.</strong> MySQL's BOOLEAN is a synonym for TINYINT(1), with TRUE = 1 and FALSE = 0. You can write either BOOLEAN or TINYINT(1); they are equivalent."
      },
      {
        "id": "mcq-13",
        "difficulty": "Medium",
        "question": "DECIMAL(7, 2) can store values up to:",
        "options": {
          "A": "9999.99",
          "B": "99999.99",
          "C": "999999.99",
          "D": "9999999.99"
        },
        "correct": "B",
        "explanation": "<strong>B is correct.</strong> DECIMAL(7,2) means 7 total digits with 2 after the decimal, leaving 5 before: max is 99999.99. For larger money amounts, use DECIMAL(10,2) or DECIMAL(15,2)."
      },
      {
        "id": "mcq-14",
        "difficulty": "Medium",
        "question": "Which is TRUE about FOREIGN KEY constraints?",
        "options": {
          "A": "They reference any column in any table",
          "B": "They prevent orphan rows by requiring the referenced value to exist in the parent table",
          "C": "They replace PRIMARY KEY",
          "D": "They are the same as UNIQUE"
        },
        "correct": "B",
        "explanation": "<strong>B is correct.</strong> Foreign keys ensure referential integrity: every child value must correspond to an existing parent row. They typically reference a PRIMARY KEY or UNIQUE column in the parent table."
      },
      {
        "id": "mcq-15",
        "difficulty": "Hard",
        "question": "Your table has a UNIQUE constraint on email. How many rows can have email = NULL in MySQL?",
        "options": {
          "A": "At most one",
          "B": "Zero (NOT NULL implied)",
          "C": "Any number",
          "D": "Depends on SQL mode"
        },
        "correct": "C",
        "explanation": "<strong>C is correct.</strong> In MySQL, UNIQUE treats multiple NULLs as not duplicates of each other, so any number of rows can have email = NULL. This differs from SQL Server's classic behavior (at most one NULL) but matches PostgreSQL and the SQL standard."
      },
      {
        "id": "mcq-16",
        "difficulty": "Hard",
        "question": "An INSERT gives \"ERROR 1452: Cannot add or update a child row: a foreign key constraint fails\". What is the cause?",
        "options": {
          "A": "A UNIQUE constraint violation",
          "B": "A primary key violation",
          "C": "The value in the FK column doesn't exist in the parent table",
          "D": "The AUTO_INCREMENT ran out of IDs"
        },
        "correct": "C",
        "explanation": "<strong>C is correct.</strong> Error 1452 means the foreign key refers to a non-existent parent row. Example: inserting an order with student_id=999 when no student 999 exists. Fix: either create the parent first, or fix the FK value."
      },
      {
        "id": "mcq-17",
        "difficulty": "Hard",
        "question": "Which statement about CHECK constraints in MySQL is TRUE?",
        "options": {
          "A": "CHECK has always been enforced in every MySQL version",
          "B": "CHECK is only enforced in MySQL 8.0.16 and later; earlier versions parsed but ignored them",
          "C": "CHECK only works with numeric columns",
          "D": "CHECK cannot be used with NOT NULL"
        },
        "correct": "B",
        "explanation": "<strong>B is correct.</strong> Historically MySQL parsed CHECK constraints for syntax compatibility but ignored them at runtime. Starting with MySQL 8.0.16 (April 2019), CHECK is properly enforced. Older MySQL installs silently accept invalid rows."
      },
      {
        "id": "mcq-18",
        "difficulty": "Hard",
        "question": "You create: <code>id INT AUTO_INCREMENT PRIMARY KEY</code> and insert 10 rows (IDs 1-10), then DELETE all rows. You then insert one row. What id does it get?",
        "options": {
          "A": "1",
          "B": "10",
          "C": "11",
          "D": "Depends"
        },
        "correct": "C",
        "explanation": "<strong>C is correct.</strong> DELETE does not reset AUTO_INCREMENT. The counter continues from where it left off, so the new row gets id=11. If you had used TRUNCATE instead of DELETE, the counter would reset to 1."
      },
      {
        "id": "mcq-19",
        "difficulty": "Hard",
        "question": "You need to store a very long article (say, 200KB of text). Best type?",
        "options": {
          "A": "VARCHAR(65535)",
          "B": "TEXT",
          "C": "MEDIUMTEXT",
          "D": "LONGTEXT"
        },
        "correct": "C",
        "explanation": "<strong>C is correct.</strong> TEXT holds up to ~64KB — 200KB won't fit. MEDIUMTEXT holds up to 16MB. LONGTEXT is overkill for 200KB but would also work. VARCHAR is capped at 65,535 bytes across the whole row, so 200KB is impossible in VARCHAR."
      },
      {
        "id": "mcq-20",
        "difficulty": "Hard",
        "question": "Which design prevents re-enrolling the same student in the same course twice?",
        "options": {
          "A": "Two separate UNIQUE constraints on student_id and course_id",
          "B": "A composite PRIMARY KEY on (student_id, course_id)",
          "C": "CHECK (student_id != course_id)",
          "D": "AUTO_INCREMENT on both columns"
        },
        "correct": "B",
        "explanation": "<strong>B is correct.</strong> A composite PRIMARY KEY on (student_id, course_id) enforces uniqueness of the PAIR, which is exactly the rule. Option A would wrongly force each student to appear only once across all enrollments and each course to appear only once — far too restrictive."
      }
    ],
    "codingChallenges": [
      {
        "id": "cc-1",
        "title": "Design a Realistic students Table",
        "difficulty": "Easy",
        "description": "Design a students table with: id (auto-increment PK), name (required, up to 60 chars), email (required, unique, up to 100 chars), phone (optional, up to 15 chars), dob (date, required), cgpa (decimal with 2 decimal places, 0 to 10, default 0), is_active (boolean defaulting TRUE), created_at (timestamp auto-set on insert).",
        "sampleInput": "(No input)",
        "sampleOutput": "A clean CREATE TABLE with the correct types and constraints.",
        "constraints": "Use all six common constraints where appropriate.",
        "solution": "CREATE TABLE students (\n    id INT AUTO_INCREMENT PRIMARY KEY,\n    name VARCHAR(60) NOT NULL,\n    email VARCHAR(100) NOT NULL UNIQUE,\n    phone VARCHAR(15),\n    dob DATE NOT NULL,\n    cgpa DECIMAL(4,2) DEFAULT 0.00 CHECK (cgpa >= 0 AND cgpa <= 10),\n    is_active BOOLEAN DEFAULT TRUE,\n    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP\n);\n\n-- Quick sanity check\nDESCRIBE students;"
      },
      {
        "id": "cc-2",
        "title": "Pick the Right Type",
        "difficulty": "Easy",
        "description": "For each real-world field, suggest the best MySQL data type and briefly justify: (a) User's age, (b) Aadhaar number, (c) Email, (d) Blog post body, (e) Product price in INR, (f) Order timestamp, (g) Employee joining date.",
        "sampleInput": "(No input)",
        "sampleOutput": "Annotated list with type + 1-line reason.",
        "constraints": "Write as SQL comments.",
        "solution": "-- (a) age           -> TINYINT UNSIGNED   (range 0-255, 1 byte)\n-- (b) aadhaar       -> CHAR(12)            (fixed 12 digits, leading zeros)\n-- (c) email         -> VARCHAR(100)        (variable, index-friendly)\n-- (d) blog body     -> MEDIUMTEXT          (can exceed 64KB)\n-- (e) price INR     -> DECIMAL(10,2)       (exact money, no float errors)\n-- (f) order time    -> TIMESTAMP           (auto time-zone, for audit)\n-- (g) joining_date  -> DATE                (year-month-day only)"
      },
      {
        "id": "cc-3",
        "title": "Employees With Constraints",
        "difficulty": "Medium",
        "description": "Create an employees table: emp_id auto-increment PK; name required up to 50 chars; email required and unique up to 100 chars; salary DECIMAL(10,2) required and strictly positive; department ENUM of 'HR','IT','Finance','Sales','Marketing' required; hire_date DATE defaulting to current date. Then insert three valid employees and attempt one invalid insert (negative salary) to show the CHECK working.",
        "sampleInput": "(No input)",
        "sampleOutput": "Three valid rows. Fourth insert raises a CHECK error.",
        "constraints": "Use CHECK and ENUM and DEFAULT.",
        "solution": "CREATE DATABASE IF NOT EXISTS hr;\nUSE hr;\nDROP TABLE IF EXISTS employees;\n\nCREATE TABLE employees (\n    emp_id INT AUTO_INCREMENT PRIMARY KEY,\n    name VARCHAR(50) NOT NULL,\n    email VARCHAR(100) NOT NULL UNIQUE,\n    salary DECIMAL(10,2) NOT NULL CHECK (salary > 0),\n    department ENUM('HR','IT','Finance','Sales','Marketing') NOT NULL,\n    hire_date DATE NOT NULL DEFAULT (CURRENT_DATE)\n);\n\n-- 3 valid inserts\nINSERT INTO employees (name, email, salary, department) VALUES\n    ('Kavya Nair',    'kavya@corp.com',  60000.00, 'HR'),\n    ('Arjun Mehta',   'arjun@corp.com',  85000.00, 'IT'),\n    ('Sneha Patel',   'sneha@corp.com',  72000.50, 'Finance');\n\n-- 1 invalid insert (negative salary)\nINSERT INTO employees (name, email, salary, department)\nVALUES ('Rahul Joshi', 'rahul@corp.com', -50000.00, 'Sales');\n-- Expected error:\n-- ERROR 3819 (HY000): Check constraint 'employees_chk_1' is violated."
      },
      {
        "id": "cc-4",
        "title": "Orders with Foreign Key",
        "difficulty": "Medium",
        "description": "Assuming employees from the previous challenge, create an orders table that links each order to an employee. Columns: order_id auto PK; emp_id INT required, FK to employees; amount decimal money positive; order_date datetime default CURRENT_TIMESTAMP. Insert 2 valid orders, then attempt an invalid one referencing a non-existent emp_id to show FK protection.",
        "sampleInput": "(No input)",
        "sampleOutput": "Two valid rows. Third insert raises FK error.",
        "constraints": "Use FOREIGN KEY REFERENCES.",
        "solution": "CREATE TABLE orders (\n    order_id INT AUTO_INCREMENT PRIMARY KEY,\n    emp_id INT NOT NULL,\n    amount DECIMAL(10,2) NOT NULL CHECK (amount > 0),\n    order_date DATETIME DEFAULT CURRENT_TIMESTAMP,\n    FOREIGN KEY (emp_id) REFERENCES employees(emp_id)\n);\n\n-- Valid\nINSERT INTO orders (emp_id, amount) VALUES (1, 1500.00);\nINSERT INTO orders (emp_id, amount) VALUES (2, 2499.99);\n\n-- Invalid: emp_id 999 does not exist\nINSERT INTO orders (emp_id, amount) VALUES (999, 500.00);\n-- Expected:\n-- ERROR 1452 (23000): Cannot add or update a child row: a foreign key constraint fails\n\nSELECT * FROM orders;"
      },
      {
        "id": "cc-5",
        "title": "DECIMAL vs FLOAT Demo",
        "difficulty": "Hard",
        "description": "Create two tables payments_decimal and payments_float that each have one column amount of type DECIMAL(10,2) and FLOAT respectively. Insert 1000 rows of 0.01 into each (you can use a stored procedure or a large VALUES list of 10 rows and run it multiple times). Sum the amounts. Observe the difference and write a final comment explaining why DECIMAL is mandatory for money.",
        "sampleInput": "(No input)",
        "sampleOutput": "total_decimal = 10.00 exact. total_float = 9.99999... or 10.000000.. imprecise.",
        "constraints": "Demonstrate with at least 100 rows to make the error visible.",
        "solution": "DROP TABLE IF EXISTS payments_decimal;\nDROP TABLE IF EXISTS payments_float;\n\nCREATE TABLE payments_decimal (amount DECIMAL(10,2));\nCREATE TABLE payments_float   (amount FLOAT);\n\n-- Insert 10 rows of 0.01 at a time, 10 times => 100 rows\nINSERT INTO payments_decimal VALUES\n (0.01),(0.01),(0.01),(0.01),(0.01),(0.01),(0.01),(0.01),(0.01),(0.01);\nINSERT INTO payments_decimal SELECT * FROM payments_decimal;\nINSERT INTO payments_decimal SELECT * FROM payments_decimal;  -- now 40 rows\nINSERT INTO payments_decimal SELECT * FROM payments_decimal;  -- now 80 rows\nINSERT INTO payments_decimal VALUES\n (0.01),(0.01),(0.01),(0.01),(0.01),(0.01),(0.01),(0.01),(0.01),(0.01),\n (0.01),(0.01),(0.01),(0.01),(0.01),(0.01),(0.01),(0.01),(0.01),(0.01);\n-- now 100 rows\n\nINSERT INTO payments_float VALUES\n (0.01),(0.01),(0.01),(0.01),(0.01),(0.01),(0.01),(0.01),(0.01),(0.01);\nINSERT INTO payments_float SELECT * FROM payments_float;\nINSERT INTO payments_float SELECT * FROM payments_float;\nINSERT INTO payments_float SELECT * FROM payments_float;\nINSERT INTO payments_float VALUES\n (0.01),(0.01),(0.01),(0.01),(0.01),(0.01),(0.01),(0.01),(0.01),(0.01),\n (0.01),(0.01),(0.01),(0.01),(0.01),(0.01),(0.01),(0.01),(0.01),(0.01);\n\nSELECT SUM(amount) AS total_decimal FROM payments_decimal;\n-- +---------------+\n-- | total_decimal |\n-- +---------------+\n-- |          1.00 |   (100 * 0.01 = exact 1.00)\n\nSELECT SUM(amount) AS total_float FROM payments_float;\n-- +----------------------+\n-- | total_float          |\n-- +----------------------+\n-- | 0.9999999776482582   |   (binary float drift!)\n\n-- CONCLUSION:\n-- Over just 100 tiny transactions the FLOAT sum is already off by ~0.0001.\n-- On 10 million real transactions, this becomes a visible mismatch.\n-- Always use DECIMAL for money."
      },
      {
        "id": "cc-6",
        "title": "Course Enrollment System",
        "difficulty": "Hard",
        "description": "Design three tables — students, courses, enrollments — for a realistic college. Include correct PRIMARY KEYs, composite PK on enrollments(student_id, course_id), FOREIGN KEYs, CHECK on marks (0-100), DEFAULT for enrollment_date, and appropriate types. Insert 3 students, 3 courses, and 5 enrollments.",
        "sampleInput": "(No input)",
        "sampleOutput": "Three tables created, populated, and a SELECT joining them at the end.",
        "constraints": "Use every constraint type you've learned.",
        "solution": "-- Reset\nDROP TABLE IF EXISTS enrollments;\nDROP TABLE IF EXISTS students;\nDROP TABLE IF EXISTS courses;\n\n-- students\nCREATE TABLE students (\n    student_id INT AUTO_INCREMENT PRIMARY KEY,\n    name VARCHAR(60) NOT NULL,\n    email VARCHAR(100) NOT NULL UNIQUE,\n    dob DATE NOT NULL\n);\n\n-- courses\nCREATE TABLE courses (\n    course_id INT AUTO_INCREMENT PRIMARY KEY,\n    course_name VARCHAR(80) NOT NULL UNIQUE,\n    credits TINYINT NOT NULL CHECK (credits BETWEEN 1 AND 6)\n);\n\n-- enrollments (junction)\nCREATE TABLE enrollments (\n    student_id INT NOT NULL,\n    course_id INT NOT NULL,\n    enrolled_on DATE NOT NULL DEFAULT (CURRENT_DATE),\n    marks TINYINT CHECK (marks BETWEEN 0 AND 100),\n    PRIMARY KEY (student_id, course_id),\n    FOREIGN KEY (student_id) REFERENCES students(student_id),\n    FOREIGN KEY (course_id)  REFERENCES courses(course_id)\n);\n\nINSERT INTO students (name, email, dob) VALUES\n    ('Aarav Sharma', 'aarav@college.edu', '2005-03-15'),\n    ('Priya Iyer',   'priya@college.edu', '2004-11-22'),\n    ('Rohan Verma',  'rohan@college.edu', '2005-07-08');\n\nINSERT INTO courses (course_name, credits) VALUES\n    ('Database Systems', 4),\n    ('Data Structures',  4),\n    ('Operating Systems', 3);\n\nINSERT INTO enrollments (student_id, course_id, marks) VALUES\n    (1, 1, 87),\n    (1, 2, 92),\n    (2, 1, 78),\n    (2, 3, 85),\n    (3, 2, 91);\n\n-- Verify with a simple JOIN (covered in Chapter 11)\nSELECT s.name, c.course_name, e.marks\nFROM enrollments e\nJOIN students s ON s.student_id = e.student_id\nJOIN courses  c ON c.course_id  = e.course_id;\n\n-- Expected: 5 rows with student name, course name, and marks."
      }
    ]
  }
}
